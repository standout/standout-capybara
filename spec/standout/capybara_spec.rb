RSpec.describe Standout::Capybara do
  it "has a version number" do
    expect(Standout::Capybara::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
